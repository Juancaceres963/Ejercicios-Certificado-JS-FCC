function palindrome(str) {
  const newStr = str.replace(/[`~!@#$%^&*()_|+\-=?;:'",.<>\{\}\[\]\\\/\s]/g, '').toLowerCase();
  const strReversed = newStr.split("").reverse().join("");

  return strReversed === newStr ? true : false ;
  
}

console.log(palindrome("five|\_/|four"));

// false
